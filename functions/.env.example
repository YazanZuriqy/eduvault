# Real secret values are set via `firebase functions:secrets:set NAME` (Secret Manager) — never
# committed here. Copy this to `.env` only for local emulator use, and keep `.env` out of git.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
